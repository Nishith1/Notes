I am building an AI-powered transformation agent inside a Spring Boot application.

Goal:
Given:
1. Input JSON
2. Expected Output JSON

The system should:
- Generate transformation rules in JSON format
- Validate the rules
- Retry if validation fails (agent loop)

Current status:
- I have created a Spring Boot app
- App is running using mvn spring-boot:run
- I have a /hello API working
- I am using VS Code

Tech constraints:
- I will use JPMC LLM Suite (not external APIs)
- I want to keep the POC simple (direct mapping + concat only)

Task:
Guide me step-by-step to:
1. Accept JSON input via API
2. Call LLM Suite
3. Generate transformation rules
4. Validate output
5. Build agent retry loop

Important:
- Give very beginner-friendly steps
- Provide Java code snippets
- Keep it simple and incremental

Let’s continue from “Step 8: Accept JSON input”.
